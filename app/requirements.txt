# DAIL Backend - Dependencies
# ─────────────────────────────────────────────────────────────
# Core Framework
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Database
# pg8000 is a pure-Python PostgreSQL driver — no C extensions, no OpenSSL.
# asyncpg and psycopg2-binary both trigger [Errno 16] on Vercel's Lambda
# runtime due to OpenSSL's /dev/urandom access being restricted.
sqlalchemy==2.0.36
pg8000==1.31.2
alembic==1.14.1

# AI / LLM
openai>=1.40.0
google-generativeai>=0.8.0

# HTTP Client
httpx==0.28.1

# Data Processing
python-multipart==0.0.20
python-dateutil==2.9.0
openpyxl==3.1.5

# Utilities
python-dotenv==1.0.1
