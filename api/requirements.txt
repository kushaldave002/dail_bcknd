# DAIL Backend - Dependencies
# ─────────────────────────────────────────────────────────────
# Core Framework
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Database
# psycopg2-binary ships as a pre-compiled wheel and works on Vercel's Lambda runtime.
# asyncpg triggers [Errno 16] Device or resource busy on Vercel — do NOT use it.
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.1

# AI / LLM
openai>=1.40.0
google-generativeai>=0.8.0

# HTTP Client
httpx==0.28.1

# Data Processing
python-multipart==0.0.20
python-dateutil==2.9.0
openpyxl==3.1.5

# Utilities
python-dotenv==1.0.1
